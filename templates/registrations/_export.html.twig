{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}
<button id="download-file" type="submit" class="w-1/2 mt-10 text-white bg-cyan-600 hover:bg-cyan-700 focus:ring-4 focus:ring-cyan-200 font-medium inline-flex items-center justify-center rounded-lg text-sm px-3 py-2 text-center sm:w-auto">Export</button>
