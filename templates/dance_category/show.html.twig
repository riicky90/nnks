{% extends 'base.html.twig' %}

{% block title %}DanceCategory{% endblock %}

{% block body %}
    <h1>DanceCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dance_category.Name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dance_category_index') }}">back to list</a>

    <a href="{{ path('dance_category_edit', {'id': dance_category.id}) }}">edit</a>

    {{ include('dance_category/_delete_form.html.twig') }}
{% endblock %}
